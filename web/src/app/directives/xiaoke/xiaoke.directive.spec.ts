import { XiaokeDirective } from './xiaoke.directive';

describe('XiaokeDirective', () => {
  it('should create an instance', () => {
    const directive = new XiaokeDirective();
    expect(directive).toBeTruthy();
  });
});
